const DATABASE_NAME = "@gameplay";

// @NomeDoApp:Coleção

const COLLECTION_USER = `${DATABASE_NAME}:user`;
const COLLECTION_APPOINTMENT = `${DATABASE_NAME}:appointments`;

export { COLLECTION_APPOINTMENT, COLLECTION_USER };
